apiVersion: v1
kind: Service
metadata:
  name: lhb-web-service
spec:
  type: NodePort
  selector:
    app: lhb-web
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000   # 브라우저에서 접근 가능한 포트
